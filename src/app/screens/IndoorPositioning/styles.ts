import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  switchContainer: {
    flexDirection: "row",
    paddingLeft: 10,
    marginTop: 10,
    alignItems: "center",
  },
});
